# 단일변수 자료: 연습문제

> 날짜: 2026-03-18
> 원본 노션: [링크](https://www.notion.so/327be135ea4a8032a52cc5b61e95a3bb)

---

# 5-1.

```r
# 1번
tabaco <- esoph$tobgp
tabaco

# 2번
levels(tabaco)

# 3번
table(tabaco)

# 4번
tabaco.bar <- table(tabaco)
barplot(tabaco.bar)
```

# 5-2.

```r
# 1번
result <- c("P","P","F","P","F","P","F","P","P","F")

# 2번
table(result)

# 3번
result.table <- table(result)
barplot(result.table)

# 4번
pie(result.table)
```

# 5-3.

```r
# 1번
season <- c("여름","겨울","봄","가을","여름","가을","겨울","여름","여름","가을")

# 2번
table(season)

# 3번
barplot(season.table)

# 4번
pie(season.table)
```

# 5-4.

```r
# 1번
score <- c(90,85,73,80,85,65,78,50,68,96)
names(score) <- c("KOR","ENG","ATH","HIST","SOC","MUSIC","BIO","EARTH","PHY","ART")

# 2번
score

# 3번
mean(score)
median(score)

# 4번
sd(score)

# 5번
names(score)[which.max(score)]

# 6번
boxplot(score)
test <- c()
if(length(boxplot.stats(score)$out)!=length(test)){
  print(boxplot.stats(score)$out)
}

# 7번
hist(score, main="학생 성적", col="purple")
```

# 5-5.

```r
# 1번
mean(mtcars$wt)
median(mtcars$wt)
mean(mtcars$wt, trim=0.15)
sd(mtcars$wt)

# 2번
summary(mtcars$wt)

# 3번
table(mtcars$cyl)

# 4번
barplot(table(mtcars$cyl))

# 5번
hist(mtcars$wt)

# 6번
boxplot(mtcars$wt)
boxplot.stats(mtcars$wt)

# 7번
boxplot(mtcars$disp)
boxplot.stats(mtcars$disp)

# 8번
boxplot(mpg~gear, data=mtcars)
# tapply(mtcars$mpg, mtcars$gear, boxplot.stats)
```

# 5-6.

```r
# 1번
head(trees)

# 2번
mean(trees$Girth)
median(trees$Girth)
mean(trees$Girth, trim=0.15)
sd(trees$Girth)

# 3번
hist(trees$Girth)

# 4번
boxplot(trees$Girth)

# 5번
mean(trees$Height)
median(trees$Height)
mean(trees$Height, trim=0.15)
sd(trees$Height)

# 6번
hist(trees$Height)

# 7번
boxplot(trees$Height)
```

# 5-7.

```r
# 1번
head(Orange)

# 2번
mean(Orange$age)
median(Orange$age)
mean(Orange$age, trim=0.15)
sd(Orange$age)

# 3번
hist(Orange$age)

# 4번
boxplot(age~Tree, data=Orange)

# 5번
mean(Orange$circumference[Orange$Tree!=2])
median(Orange$circumference[Orange$Tree!=2])
mean(Orange$circumference[Orange$Tree!=2], trim=0.15)
sd(Orange$circumference[Orange$Tree!=2])

# 6번
hist(Orange$circumference[Orange$Tree!=2])

# 7번
boxplot(circumference~Tree, data=Orange)
```

