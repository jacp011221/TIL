# 데이터 전처리: 연습문제

> 날짜: 2026-03-22
> 원본 노션: [링크](https://www.notion.so/32abe135ea4a8094a80ee2210f9c54b8)

---

# 7-1.

```r
# 1번
apply(is.na(ds), 2, sum)

# colSums(is.na(ds))

# 2번
ds[!complete.cases(ds),]

# 3번
nrow(ds[!complete.cases(ds),])

# 4번
ds.new <- ds[complete.cases(ds),]
```

# 7-2.

```r
# 1번
par(mfrow=c(3,4))
for(i in 1:ncol(mt)){
  boxplot(mt[,i],
          main=colnames(mt[i]))
}
par(mfrow=c(1,1))

# 2번
for(i in 1:ncol(mt)){
  mt[,i][mt[,i] %in% boxplot.stats(mt[,i])$out] <- NA
}

# for(i in 1:ncol(mt)){
#   mt_out <- boxplot.stats(mt[,i])$out
#   mt[mt[,i] %in% mt_out, i] <- NA
# }

# 3번
mt2 <- mt[complete.cases(mt),]
```

# 7-3.

```r
# 1번
AQ <- airquality

# 2번
colSums(is.na(AQ))

# 3번
rowSums(is.na(AQ))

# 4번
AQ[complete.cases(AQ),]

# 5번
ozmean <- colMeans(AQ[complete.cases(AQ),])[1]
# 잘못됨. 다른 열에만 결측치가 존재하는 경우에도 행이 제외되기 때문에 평균이 달라진다.
# >>
# ozmean <- mean(AQ[,1], na.rm=T)
solmean <- colMeans(AQ[complete.cases(AQ),])[2]
# 잘못됨. 다른 열에만 결측치가 존재하는 경우에도 행이 제외되기 때문에 평균이 달라진다.
# >>
# solmean <- mean(AQ[,2], na.rm=T)

AQ2 <- AQ

AQ2[!complete.cases(AQ[,1]),1] <- ozmean
AQ2[!complete.cases(AQ[,2]),2] <- solmean
AQ2
```

# 7-4.

```r
# 1번
x77 <- data.frame(state.x77)
x77[order(x77$Population),]

# 2번
x77[order(x77$Income, decreasing = T),]

# 3번
newx77 <- x77[order(x77$Illiteracy),]
head(newx77, 10)["Illiteracy"]
```

# 7-5.

```r
# 1번
mt.gear <- split(mtcars, mtcars$gear)

# 2번
mt.gear["4"]

# 3번
# mt.gear.35 <- do.call(rbind, mt.gear[c("3","4")])
# rownames(mt.gear.35) <- sub("^[0-9]+\\.","",rownames(mt.gear.35))
# mt.gear.35

# 4번
subset(mtcars, 1.5<wt & wt<3.0)
```

# 7-6.

```r
# 1번
library(mlbench)
data("Glass")
myds <- Glass

# 2번
aggregate(myds[c("RI","Na","Mg","Al","Si")],
          by=list(myds$Type),
          FUN=mean)
```

# 7-7.

```r
# 1번
library(mlbench)
data("Ionosphere")
myds <- Ionosphere

# 2번
aggregate(myds[,3:10],
          by=list(V1=myds$V1, Class=myds$Class),
          FUN=sd)
```

# 7-8.

```r
set.seed(100)
mt.sample <- sample(1:nrow(mtcars),size=10,
               replace = FALSE)

mt10 <- mtcars[mt.sample,]
mt.other <- mtcars[-mt.sample,]
mt10
mt.other
```

# 7-9.

```r
for(i in 1:3){
  iris.spe <- subset(iris, Species==levels(iris$Species)[i])
  set.seed(100)
  sam <- sample(1:50,size=10,
                    replace = FALSE)
  if(i==1){
    iris.10 <- iris.spe[sam,]
  }else{
    iris.10 <- rbind(iris.10,iris.spe[sam,])
  }
}
iris.10
```

# 7-10.

```r
set.seed(100)
sam <- sample(1:nrow(cars),size=nrow(cars),
       replace = FALSE)
cars.2 <- cars[sam,]
cars <- cars.2
cars
```

# 7-11.

```r
combn(levels(iris$Species),2)
```

# 7-12.

```r
fruit <- c("banana","apple","melon","peach","mango")
combn(fruit,3)
```

# 7-13.

```r
x77 <- data.frame(state.x77)
subset(x77, x77["Alabama","Area"]<Area &
         Area<x77["California","Area"])[c("Population","Income","Area")]
```

# 7-14.

```r
# 1번
setwd("작업 폴더의 경로 이름")
# subway <- read.csv("subway.csv", header = T, fileEncoding = "euc-kr")
# subway_latlong <- read.csv("subway_latlong.csv", header = T, fileEncoding = "euc-kr")
# subway.tot <- merge(subway,subway_latlong,
#                     by.x="station",by.y="STATION_CD")

# 2번
aggregate(subway.tot[c("on_tot","off_tot")],
          by=list(역.이름=subway.tot$stat_name,날짜=subway.tot$income_date),
          FUN=sum)

# 3번
totdate <- 20110101<=subway.tot$income_date &
  subway.tot$income_date<=20111231

subway.tot.2011 <- subway.tot[totdate,]

aggregate(subway.tot.2011[c("on_tot","off_tot")],
          by=list(역.이름=subway.tot.2011$stat_name),
          FUN=sum)

# 4번
aggregate(subway.tot.2011[c("on_tot","off_tot")],
          by=list(호선=subway.tot.2011$LINE_NUM),
          FUN=sum)
```

# 7-15.

```r
# 1번
authors <- data.frame(
  surname = c("Twein","Venables","Tierney","Ripley","McNeil"),
  nationality = c("US","Australia","US","UK","Australia"),
  retired = c("yes", rep("no",4)))
books <- data.frame(
  name = c("Johns","Venables","Tierney","Ripley","Ripley",
            "McNeil"),
  title = c("Exploratory Data Analysis",
             "Modern Applied Statistics ...",
             "LISP-STAT",
             "Spatial Statistics", "Stochastic Simulation",
             "Interactive Data Analysis"),
  other.author = c(NA,"Ripley",NA,NA,NA,NA))
authors
books

# 2번
merge(authors,books,
      by.x="surname", by.y="name")

# 3번
merge(authors,books,
      by.x="surname", by.y="name",
      all.x = T)

# 4번
merge(authors,books,
      by.x="surname", by.y="name",
      all.y = T)

# 5번
merge(authors,books,
      by.x="surname", by.y="name",
      all = T)
```

