# 참조 타입

> 날짜: 2026-04-05
> 원본 노션: [링크](https://www.notion.so/339be135ea4a80e88ee9c0d971e3c1d3)

---

# 0. 시작하기

- Shell: 터미널 명령어 실행 환경
```bash
ls -al   # ls: 파일 목록 보기 / -a: 숨김 파일 포함(all) / -l: 상세 정보 표시
nano ~/.zshrc   # nano: 터미널에서 파일을 확인/수정하는 텍스트 편집기
source ~/.zshrc   # source: 파일 내용 적용

javac -version   # javac 버전 확인
java -version   # java 버전 확인
```

- JDK(Java Development Kit): JVM, javac 등의 개발 도구들을 포함한 묶음
- 소스 파일(.java) > 컴파일(javac) > 바이트코드 파일(.class) > 실행(java)
```java
// ~/temp
//    |- src            // 소스 파일이 저장되는 디렉토리
//    |   |- ch01       // 패키지 디렉토리
//    |      |- sec06   // 패키지 디렉토리
//    |         |- Hello.java
//    |
//    |- bin            // 바이트코드 파일이 저장되는 디렉토리

package ch01.sec06;   // 클래스가 속한 패키지 선언

public class Hello {                           // Hello 클래스 선언
    public static void main(String[] args) {   // main() 메소드 선언
        System.out.println("Hello, Java");     // 콘솔에 문자열을 출력하는 실행문
    }
}
```

- 패키지: 클래스를 관리하기 편하도록 디렉토리 구조와 일치하게 구성한 논리적 그룹
- 클래스: 객체를 생성하기 위한 틀로, 데이터와 기능(메소드)을 포함
- 메소드: 클래스에 정의된 기능
- 실행문: 프로그램 실행 시 실제로 수행되는 명령을 나타내는 코드
```bash
cd ~/temp   # cd: 디렉토리로 이동
javac -d bin src/ch01/sec06/Hello.java   # javac: .java 컴파일
# bin: 바이트코드 파일 저장 위치 / src/ch01/sec06/Hello.java: 소스 경로+파일명

java -cp bin ch01.sec06.Hello   # java: .class 실행 / -cp: 클래스패스 지정
# bin: 바이트코드 파일 저장 위치 / ch01.sec06.Hello: 패키지+클래스명
```

```java
// 행 주석, 행 끝까지 주석으로 처리

/*
범위 주석
사이 내용은 전부 주석으로 처리
 */

/**
 * 도큐먼트 주석
 * 사이 내용은 전부 주석으로 처리
 * javadoc 명령어로 API 도큐먼트를 생성하는 데 사용
 */
```

```bash
javadoc Hello.java   # 도큐먼트 주석을 기반으로 HTML 문서를 자동으로 생성해 주는 Shell 명령어
```

- API 도큐먼트: 클래스/메소드 사용법을 정리한 설명서
# 1. 변수와 타입

- 변수: 하나의 값을 저장할 수 있는 메모리 공간에 붙여진 이름
- 캐멀 스타일: 단어 구분을 대소문자의 차이로 보여주는 스타일
```java
package ch02.sec01;

public class VariableUseExample {
    public static void main(String[] args) {
        int hour;   // 정수형 변수 hour 선언
        hour = 3;   // 변수 초기화(hour에 값 대입, 메모리 할당)
        // 변수 초기화를 실행하지 않으면 컴파일 에러가 발생한다.
        int minute = 5;
        System.out.println(hour + "시간 " + minute + "분");   // 문자열과 결합

        int totalMinute = (hour*60) + minute;   // 연산식에서 변수 활용
        System.out.println("총 " + totalMinute + "분");
    }
}
```

## 1-1. 정수 타입

허용 범위를 초과하는 값을 대입할 경우 컴파일 에러가 발생한다.

- bit: 0과 1이 저장되는 최소 단위
- n개의 bit로 구성된 정수 타입은 최상위 bit를 부호 bit로 하고, 나머지 bit가 값의 범위를 결정한다.
- 리터럴: 프로그래머가 직접 변수에 대입한 값
```java
package ch02.sec02;

public class IntegerLiteralExample {
    public static void main(String[] args) {
        int var1 = 0b1011;   // 2진수
        int var2 = 0206;     // 8진수
        int var3 = 365;      // 10진수
        int var4 = 0xB3;     // 16진수
        long varLong = 1000000000000L;

        System.out.println("var1: " + var1);         // 11
        System.out.println("var2: " + var2);         // 134
        System.out.println("var3: " + var3);         // 365
        System.out.println("var4: " + var4);         // 179
        System.out.println("varLong: " + varLong);   // 1000000000000
    }
}
```

## 1-2. 문자 타입

- 문자 리터럴: 하나의 문자를 작은따옴표(’)로 감싼 것으로, 유니코드로 변환되어 저장된다.
```java
package ch02.sec03;

public class CharExample {
    public static void main(String[] args) {
        char c1 = 'A';   // 문자 저장
        char c2 = 65;    // 유니코드 직접 저장

        char c3 = '가';     // 문자 저장
        char c4 = 44032;   // 유니코드 직접 저장

        System.out.println(c1);   // A
        System.out.println(c2);   // A
        System.out.println(c3);   // 가
        System.out.println(c4);   // 가
    }
}
```

## 1-3. 실수 타입

- 실수는 IEEE 754 표준에 근거한 부동 소수점 방식으로 저장된다.
- 10의 거듭제곱 리터럴: e 또는 E를 포함하여 작성
- 컴파일러는 기본적으로 실수 리터럴을 double 타입 값으로 간주하기 때문에 float 타입을 사용하고 싶다면 리터럴 뒤에 f 또는 F를 붙여 float 타입 값임을 명시해야 한다.
## 1-4. 논리 타입

- boolean 타입
- 논리 리터럴
- 비교/논리 연산의 산출값은 true 혹은 false이므로 boolean 타입 변수에 대입할 수 있다.
## 1-5. 문자열 타입

- String 타입
```java
package ch02.sec06;

public class TextBlockExample {
    public static void main(String[] args) {
        String str1 = "" +
                "{\n" +
                "\t\"id\":\"winter\",\n" +
                "\t\"name\":\"눈송이\"\n" +
                "}";
        String str2 = """
                {
                    "id":"winter",
                    "name":"눈송이"
                }
                """;
        System.out.println(str1);
        System.out.println("----------------------------------------");
        System.out.println(str2);
        System.out.println("----------------------------------------");
        // """로 감싼 텍스트 블록은 대부분의 문자가 이스케이프하거나 라인피드할 필요 없이
        // 작성한 문자열 그대로 저장된다.
        
        String str3 = """
                나는 자바를 \
                학습합니다.
                나는 자바 고수가 될 겁니다.
                """;   // 텍스트 블록에서 라인의 끝에 \를 붙이면 줄바꿈 없이 이어서 작성된다.
        System.out.println(str3);
    }
}
```

## 1-6. 타입 변환

- 자동 타입 변환: 값의 허용 범위가 작은 타입이 큰 타입에 대입될 때 발생
- 강제 타입 변환(캐스팅): 값의 허용 범위가 큰 타입을 작은 타입에 대입할 때 사용
```java
package ch02.sec08;

public class CastingExample {
    public static void main(String[] args) {
        int var1 = 10;
        byte var2 = (byte) var1;
        System.out.println(var2);

        long var3 = 300;
        int var4 = (int) var3;
        System.out.println(var4);

        int var5 = 65;
        char var6 = (char) var5;
        System.out.println(var6);

        double var7 = 3.14;
        int var8 = (int) var7;
        System.out.println(var8);
    }
}
```

- 연산식 자동 타입 변환
- 문자열-기본 타입 변환
## 1-7. 콘솔로 변수 출력

```java
System.out.println(리터럴 또는 변수);
// System.: 시스템으로
// out.: 출력하는데
// println();: 괄호 안의 내용을 출력하고 행을 바꿔라
```

- 형식 문자열
## 1-8. 키보드 입력 데이터 저장

```java
package ch02.sec13;

import java.util.Scanner;   // java.util 패키지에 있는 Scanner 클래스 호출

public class ScannerExample {
    public static void main(String[] args) throws Exception {
    // throws Exception: 예외를 호출한 쪽으로 넘김
        Scanner scanner = new Scanner(System.in);
        // new 연산자로 Scanner 객체를 생성하고 변수 scanner에 대입

        System.out.print("x 값 입력: ");
        String strX = scanner.nextLine();
        // Enter 키가 입력되기 전까지 블로킹(대기) 상태가 되며,
        // Enter 키가 입력되면 그 이전까지 입력된 모든 내용을 strX 문자열에 저장한다.
        int x = Integer.parseInt(strX);

        System.out.print("y 값 입력: ");
        String strY = scanner.nextLine();
        int y = Integer.parseInt(strY);

        int result = x + y;
        System.out.println("x + y: " + result);
        System.out.println();

        while(true) {
            System.out.print("입력 문자열: ");
            String data = scanner.nextLine();
            if(data.equals("q")) {   // equals(): String 값이 동일한지 비교하는 메소드
                break;
            }
            System.out.println("출력 문자열: " + data);
            System.out.println();
        }

        System.out.println("종료");
    }
}
```

# 2. 연산자

## 2-1. 부호/증감 연산자

- 부호 연산자: 변수의 부호를 유지하거나 변경
- 증감 연산자: 변수의 값을 1 증가 혹은 1감소
## 2-2. 산술 연산자

- 정확한 산술 연산을 원한다면 실수 타입보다는 정수 타입으로 계산하는 것이 낫다.
- 나눗셈(/)혹은 나머지(%) 연산에서 우측 피연산자가 0일 경우
## 2-3. 오버플로우/언더플로우

- 오버플로우: 타입이 허용하는 최대값을 벗어나는 것
- 언더플로우: 타입이 허용하는 최소값을 벗어나는 것
- 실행 에러는 발생하지 않으며, 최소값/최대값으로 돌아가 순환한다.
## 2-4. 비교 연산자

- 비교식을 통해 boolean 타입의 값을 반환한다.
- 피연산자의 타입이 서로 다를 경우 비교 연산을 수행하기 전 자동 변환을 통해 타입을 일치시킨다.
## 2-5. 논리 연산자

- 논리식을 통해 boolean 타입의 값을 반환한다.
## 2-6. 비트 연산자

- 정수 타입만 피연산자가 될 수 있고, 연산 결과는 int 타입(long 타입이 포함되었을 경우 long)으로 반환된다.
- 비트 논리 연산자는 1은 true, 0은 false라고 생각했을 때 논리 연산자와 동일하다.
```java
package ch03.sec08;

public class BitLogicExample {
    public static void main(String[] args) {
				int test = 136;   // 원래 값 136
        byte receiveData = (byte) test;   // 136을 byte 타입으로 강제 변환한 값 -120
        
        int unsignedInt1 = receiveData & 0xFF;   // 원래 값 136 복원
        System.out.println(unsignedInt1);

        int unsignedInt2 = Byte.toUnsignedInt(receiveData);   // 원래 값 136 복원
        System.out.println(unsignedInt2);
        
        int num1 = 1;
        int result1 = num1 << 3;
        int result2 = num1 * (int) Math.pow(2, 3);
        // Math.pow(): 2³을 연산하고, double 값을 산출
        System.out.println("result1: " + result1);
        System.out.println("result2: " + result2);

        int num2 = -5;
        int result3 = num2 >> 1;
        int result4 = num2 / 2;
        System.out.println("result3: " + result3);   // -3
        System.out.println("result4: " + result4);   // -2
    }
}
```

## 2-7. 대입 연산자

- 우측 피연산자(리터럴 / 변수 / 다른 연산식)의 값을 좌측 피연산자인 변수에 대입
## 2-8. 삼항 연산자(조건 연산자)

- 조건식 ? (값 또는 연산식) : (값 또는 연산식)
## 2-9. 연산의 방향과 우선순위

- 괄호()는 연산에 있어 최우선순위를 가지며, 괄호가 없는 상황에서의 우선순위는 다음과 같다.
- 객체 instanceof 타입: true/false 값을 가지는 논리 연산자로, 객체가 해당 타입 혹은 하위 타입인가를 확인하는 연산자이다.
# 3. 제어문

- 제어문: 실행 흐름을 개발자가 원하는 방향으로 바꿀 수 있도록 해 주는 문장
## 3-1. if문

- 조건식의 결과에 따라 블록 실행 여부가 결정되는 조건문
- if-else문: true이면 if문 블록 실행, false이면 else 블록 실행
- else if: if문과 else 블록 사이에 존재하는 블록으로, 상위 조건식이 false일 경우 평가되어 true일 경우 실행, false일 경우 다음 블록으로 진행
```java
package ch04.sec02;

public class IfDiceExample {
    public static void main(String[] args) {
        int num = (int)(Math.random()*6) + 1;
        // Math.random(): 0.0 <= ~ < 1.0 사이의 double 타입 난수 리턴
        
        if(num==1) {
            System.out.println("1번이 나왔습니다.");
        } else if(num==2) {
            System.out.println("2번이 나왔습니다.");
        } else if(num==3) {
            System.out.println("3번이 나왔습니다.");
        } else if(num==4) {
            System.out.println("4번이 나왔습니다.");
        } else if(num==5) {
            System.out.println("5번이 나왔습니다.");
        } else {
            System.out.println("6번이 나왔습니다.");
        }
    }
}
```

## 3-2. switch문

- 괄호 안의 변수값과 일치하는 case의 실행문을 실행하는 조건문
- 업데이트
```java
package ch04.sec03;

public class SwitchValueExample {
    public static void main(String[] args) {
        String grade = "B";

        int score1 = 0;
        switch(grade) {
            case "A":
                score1 = 100;
                break;
            case "B":
                int result = 100 - 20;
                score1 = result;
                break;
            default:
                score1 = 60;
        }
        System.out.println("score1: " + score1);

        int score2 = switch(grade) {
            case "A" -> 100;
            case "B" -> {
                int result = 100 - 20;
                yield result;
            }
            default -> 60;
        };
        System.out.println("score2: " + score2);
    }
}
```

## 3-3. for문

- 조건식이 참인 동안 실행문을 정해진 횟수만큼 반복 실행하는 반복문
- 초기화식에서 부동 소수점을 사용하는 float 타입의 사용은 지양하는 것이 좋다.
```java
package ch04.sec04;

public class MultiplicationTableExample {
    public static void main(String[] args) {
        for (int m=2; m<=9; m++) {
            System.out.println("*** " + m + "단 ***");
            for (int n=1; n<=9; n++) {
                System.out.println(m + " x " + n + " = " + (m*n));
            }
        }
    }
}
```

## 3-4. while문

- 조건식이 참인 동안 실행문을 반복 실행하는 반복문
```java
package ch04.sec05;

import java.util.Scanner;

public class KeyControlExample {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        boolean run = true;
        int speed = 0;

        while(run) {
            System.out.println("-------------------------");
            System.out.println("1. 증속 | 2. 감속 | 3. 중지");
            System.out.println("-------------------------");
            System.out.println("선택 : ");

            String strNum = scanner.nextLine();

            if(strNum.equals("1")) {
                speed++;
                System.out.println("현재 속도 = " + speed);
            } else if(strNum.equals("2")) {
                speed--;
                System.out.println("현재 속도 = " + speed);
            } else if(strNum.equals("3")) {
                run = false;
            }
        }

        System.out.println("프로그램 종료");
    }
}
```

## 3-5. do-while문

- 블록 내부를 실행시킨 후 조건식을 평가한다. 이 외에는 while문과 동일하다.
```java
package ch04.sec06;

import java.util.Scanner;

public class DoWhileExample {
    public static void main(String[] args) {
        System.out.println("메시지를 입력하세요.");
        System.out.println("프로그램을 종료하려면 q를 입력하세요.");

        Scanner scanner = new Scanner(System.in);
        String inputString;

        do {
            System.out.print(">");
            inputString = scanner.nextLine();
        } while( !inputString.equals("q") );

        System.out.println();
        System.out.println("프로그램 종료");
    }
}
```

## 3-6. break문

- 가장 가까운 반복문 혹은 switch문을 종료하기 위해 사용
```java
package ch04.sec07;

public class BreakOutterExample {
    public static void main(String[] args) throws Exception {
        Outter: for(char upper='A'; upper<='Z'; upper++) {
            for(char lower='a'; lower<='z'; lower++) {
                System.out.println(upper + "-" + lower);
                if(lower=='g') {
                    break Outter;
                }
            }
        }
        System.out.println("프로그램 실행 종료");
    }
}
```

## 3-7. continue문

- 반복문의 블록 내부에서 사용하여 이후의 실행문을 무시하고 증감식/조건식으로 돌아가 반복
```java
package ch04.sec08;

public class ContinueExample {
    public static void main(String[] args) throws Exception {
        for(int i=1; i<=10; i++) {
            if(i%2 != 0) {
                continue;
            }
            System.out.print(i + " ");
        }
    }
}
```

# 4. 참조 타입

- Java의 데이터 타입은 크게 기본 타입과 참조 타입으로 나뉜다.
- 객체: 필드(속성, 데이터) + 메소드(동작)
- 메모리 영역: java 명령어로 JVM이 구동될 때 운영체제에서 할당받은 메모리 영역을 나눠서 사용
- 스레드: 프로그램 안에서의 실행 흐름
```java
package ch05.sec03;

public class ReferenceVariableCompareExample {
    public static void main(String[] args) {
        int[] arr1;
        int[] arr2;
        int[] arr3;

        arr1 = new int[] { 1, 2, 3 };
        arr2 = new int[] { 1, 2, 3 };
        arr3 = arr2;

        System.out.println(arr1 == arr2);   // false
        System.out.println(arr2 == arr3);   // true
    }
}
```

- 참조 타입 변수를 초기화할 때 null값을 넣어 객체를 참조하지 않는 상태로 초기화할 수 있다.
- 어떤 변수에서도 참조하지 않는 객체는 쓰레기가 되어 Java의 쓰레기 수집기에 의해 자동으로 제거된다.
## 4-1. 문자열 타입

- 문자열 리터럴이 동일하다면 같은 객체를 공유한다.
- String 객체의 메모리 주소와 관계없이 내부 문자열 값을 비교하길 원한다면 equals() 메소드 사용
- char 타입과는 달리 빈 문자열(””)을 대입할 수 있다.
```java
package ch05.sec05;

public class CharAtExample {
    public static void main(String[] args) {
        String strVar1 = "홍길동";
        String strVar2 = "홍길동";
        String strVar3 = new String("홍길동");
        // strVar1과 strVar2는 같은 객체를 참조하고, strVar3은 새로운 객체로 생성된다.
    
        String ssn1 = "9506241230123";
        char sex = ssn1.charAt(6);   // charAt(x): x번 인덱스의 문자
        // 인덱스는 0번부터 시작하여 길이-1번까지 존재한다.
        switch (sex) {
            case '1':
            case '3':
                System.out.println("남자입니다.");
                break;
            case '2':x
            case '4':
                System.out.println("여자입니다.");
                break;
        }

        int length = ssn1.length();   // length(): 문자열의 길이(문자의 개수)
        if(length == 13) {
            System.out.println("주민등록번호 자릿수가 맞습니다.");
        } else {
            System.out.println("주민등록번호 자릿수가 틀립니다.");
        }
        
        String oldStr = "자바 문자열은 불변입니다. 자바 문자열은 String입니다.";
        String newStr = oldStr.replace("자바","JAVA");
        // replace(x,y): 문자열 내부의 x를 y로 변환하여 새 문자열 반환(기존 문자열 변환이 아님)
        
        String ssn2 = "880815-1234567";
        String firstNum = ssn2.substring(0,6);
        // substring(x,y): x번 인덱스부터 y번 인덱스 바로 앞(y-1번 인덱스)까지 잘라내어 반환
        String secondNum = ssn2.substring(7);   // y를 생략 시 x번 인덱스부터 끝까지
        
        String subject = "자바 프로그래밍";
        int location = subject.indexOf("프로그래밍");
        // indexOf(x): 문자열 x가 시작되는 위치의 인덱스 값, 없으면 -1 반환
        System.out.println(location);
        String substring = subject.substring(location);
        System.out.println(substring);

        location = subject.indexOf("자바");
        if(location != -1) {
            System.out.println("자바와 관련된 책이군요.");
        } else {
            System.out.println("자바와 관련 없는 책이군요.");
        }

        boolean result = subject.contains("자바");
        // contains(x): 문자열 x가 들어 있는지 확인하는 메소드, boolean 값 반환
        if(result) {
            System.out.println("자바와 관련된 책이군요.");
        } else {
            System.out.println("자바와 관련 없는 책이군요.");
        }
        
        String board = "1,자바 학습,참조 타입 String을 학습합니다.,홍길동";
        String[] tokens = board.split(",");
        // split(x): x를 기준으로 문자열을 분리하여 String 배열로 반환
                
        for(int i=0; i<tokens.length; i++) {
            System.out.println(tokens[i]);
        }
    }
}
```

## 4-2. 배열 타입

- 연속된 메모리 공간에 값을 나열시키고, 각 값에 인덱스를 부여해 놓은 1차원 자료구조
```java
package ch05.sec06;

public class ArrayCreateByValueListExample1 {
    public static void main(String[] args) {
        String[] season = { "Spring", "Summer", "Fall", "Winter" };
        // 문자열 타입의 season 배열 생성
        season[1] = "여름";   // "Summer"를 "여름"으로 변경

        int[] scores;
        scores = new int[] { 83, 90, 87 };
        // 배열 변수를 선언한 시점과 값 목록 대입 시점이 다르면 new 타입[]을 중괄호 앞에 붙인다.
        printItem( new int[] { 83, 90, 87 } );
        // 메소드의 매개변수가 배열 타입일 경우에도 위와 동일하게 처리한다.
    }

    public static void printItem( int[] scores ) {   // printItem() 메소드 선언
        for(int i=0; i<3; i++) {
            System.out.println("score[" + i + "]: " + scores[i]);
        }
    }
}
```

- new 연산자로 배열을 생성하면 배열 항목은 기본값으로 초기화된다.
```java
package ch05.sec06;

public class ArrayCreateByNewExample {
    public static void main(String[] args) {
        int[] arr1 = new int[3];   // int 타입의 길이 3인 배열 arr1 생성
        arr1[0] = 10;   // 바로 값 대입이 가능하다.
        
        int[] scores = { 84, 90, 96 };
        int sum = 0;
        for(int i=0; i<scores.length; i++) {
        // length: 배열의 length 필드(길이)를 읽는다. 메소드가 아니며, 값을 변경할 수는 없다.
            sum += scores[i];
        }
        System.out.println("총합 : " + sum);
    }
}
```

## 4-3. 다차원 배열

- 배열의 요소가 또 다른 배열을 참조하는 구조 (n차원 구조)
```java
package ch05.sec07;

public class MultidimensionalArrayByValueListExample {
    public static void main(String[] args) {
        int[][] scores = {   // 다차원 배열 scores 생성
                { 80, 90, 96 },
                { 76, 88 }
        };

        System.out.println("1차원 배열 길이: " + scores.length);
        System.out.println("2차원 배열 길이[0]: " + scores[0].length);
        System.out.println("2차원 배열 길이[1]: " + scores[1].length);

        System.out.println("scores[0][2]: " + scores[0][2]);
        System.out.println("scores[1][1]: " + scores[1][1]);
        
        
        int[][] mathScores = new int[2][3];   // new 연산자를 활용한 다차원 배열 생성
        mathScores[0][0] = 80;
        mathScores[0][1] = 83;
        mathScores[0][2] = 85;
        mathScores[1][0] = 86;
        mathScores[1][1] = 90;
        mathScores[1][2] = 92;

        int[][] englishScores = new int[2][];
        // 1차원 배열 객체 우선 생성(2차원 배열의 길이가 서로 다르거나, 아직 모를 때)
        englishScores[0] = new int[2];
        englishScores[1] = new int[3];
    }
}
```

## 4-4. 배열 복사

```java
package ch05.sec09;

public class ArrayCopyExample {
    public static void main(String[] args) {
        int[] oldIntArray = { 1, 2, 3 };
        int[] newIntArray = new int[5];
        
        // for문을 활용한 복사
        for(int i=0; i<oldIntArray.length; i++) {
            newIntArray[i] = oldIntArray[i];
        }

        // System.arraycopy() 메소드를 활용한 복사
        System.arraycopy( oldIntArray, 0, newIntArray, 0, oldIntArray.length);
        // System.arraycopy(): 원본 배열, 원본 배열 복사 시작 인덱스,
        // 새 배열, 새 배열 붙여넣기 시작 인덱스, 복사 항목 수
    }
}
```

## 4-5. 배열 항목 반복을 위한 for문

- 배열에서 가져올 항목이 존재하는 동안 반복변수에 배열 값을 순서대로 대입하는 형태의 for문
```java
package ch05.sec10;

public class AdvancedForExample {
    public static void main(String[] args) {
        int[] scores = { 95, 71, 84, 93, 87 };
        int sum = 0;
        for (int score : scores) {   // 반복변수, 배열
            sum = sum + score;
        }
        System.out.println("점수 총합 = " + sum);
    }
}
```

## 4-6. main() 메소드의 String[] 매개변수

- 프로그램을 실행할 때 입력받은 값이 문자열로 저장되는 배열 
- IntelliJ에서 입력값을 주고 실행하는 법
```java
package ch05.sec11;

public class MainStringArrayArgument {
    public static void main(String[] args) {
        if(args.length != 2) {
            System.out.println("프로그램 입력값이 부족");
            System.exit(0);
        }

        String strNum1 = args[0];
        String strNum2 = args[1];

        int num1 = Integer.parseInt(strNum1);
        int num2 = Integer.parseInt(strNum2);

        int result = num1 + num2;
        System.out.println(num1 + " + " + num2 + " = " + result);
    }
}
```

- 터미널에서 실행하는 법은 다음과 같다.
```bash
# 클래스패스까지 이동 후 실행
java ch05.sec11.MainStringArrayArgument 10 20   # 입력값을 주고 실행
```

## 4-7. 열거 타입

- 한정된 값을 갖는 타입으로, 대개 별도의 소스 파일에 정의한다.
- 열거 상수(값 목록)는 관례적으로 대문자 알파벳으로 정의하며, 여러 단어로 구성될 경우에는 단어와 단어 사이를 언더바(_)로 연결한다. (e.g., LOGIN_SUCCESS)
```java
package ch05.sec12;

public enum Week {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY
}
```

```java
package ch05.sec12;

import java.util.Calendar;   // Calendar 클래스 호출

public class WeekExample {
    public static void main(String[] args) {
        Week today = null;   // 열거 타입은 참조 타입이므로 null을 대입할 수 있다.

        Calendar cal = Calendar.getInstance();   // cal 변수에 Calendar 객체 대입
        int week = cal.get(Calendar.DAY_OF_WEEK);   // 일(1)~토(7)

        switch(week) {
            case 1: today = Week.SUNDAY;    break;   // 열거타입.열거상수 형태로 대입
            case 2: today = Week.MONDAY;    break;
            case 3: today = Week.TUESDAY;   break;
            case 4: today = Week.WEDNESDAY; break;
            case 5: today = Week.THURSDAY;  break;
            case 6: today = Week.FRIDAY;    break;
            case 7: today = Week.SATURDAY;  break;
        }

        if(today == Week.SUNDAY) {
            System.out.println("일요일에는 게임을 합니다.");
        } else {
            System.out.println("열심히 자바를 공부합니다.");
        }
    }
}
```

# X. 기타

- Calendar: 컴퓨터의 날짜 / 시간 / 요일 등을 얻을 때 사용하는 클래스
