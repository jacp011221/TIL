# 중첩 선언과 익명 객체: 연습문제

> 날짜: 2026-04-09
> 원본 노션: [링크](https://www.notion.so/33dbe135ea4a80048990f712d8569ad5)

---

# 9-4.

```java
package ch09.sec00.test04;

public class CarExample {
    public static void main(String[] args) {
        Car myCar = new Car();
        Car.Tire tire = myCar.new Tire();
        Car.Engine engine = new Car.Engine();
    }
}
```

# 9-5.

```java
package ch09.sec00.test05;

public class ActionExample {
    public static void main(String[] args) {
        Action action = new Action() {
            @Override
            public void work() {
                System.out.println("복사를 합니다.");
            }
        };
        action.work();
    }
}
```

# 9-6.

```java
// Anonymous
package ch09.sec00.test06;

public class Anonymous {
    Vehicle field = new Vehicle() {
        @Override
        public void run() {
            System.out.println("자전거가 달립니다.");
        }
    };
    void method1() {
        Vehicle localVar = new Vehicle() {
            @Override
            public void run() {
                System.out.println("승용차가 달립니다.");
            }
        };
        localVar.run();
    }

    void method2(Vehicle v) {
        v.run();
    }
}
```

```java
// AnonymousExample
package ch09.sec00.test06;

public class AnonymousExample {
    public static void main(String[] args) {
        Anonymous anony = new Anonymous();
        anony.field.run();
        anony.method1();
        anony.method2(new Vehicle() {
            @Override
            public void run() {
                System.out.println("트럭이 달립니다.");
            }
        });
    }
}
```

# 9-7.

```java
// 익명 자식 객체 chat 내부의 while문을 빠져나올 코드가 없기 때문에 에러가 발생한다.
// ! 로컬 클래스에서 사용하는 로컬 변수인 nickName이 final 특성을 가짐에도 초기값에서 변경한 것이
//   컴파일 에러의 원인이다.
```

